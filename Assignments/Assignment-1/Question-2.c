#include <conio.h>
#include <stdio.h>

int main() {
    printf("The size of int: %ld bytes\n", sizeof(int));
    printf("The size of char: %ld bytes\n", sizeof(char));
    printf("The size of long long into %ld bytes\n", sizeof(long long int));
    printf("The size of unsigned int : %ld bytes\n", sizeof(unsigned int));
    return(0);
}